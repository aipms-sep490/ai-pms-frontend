import { cleanup, render, screen } from '@testing-library/react'
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { MemoryRouter } from 'react-router-dom'
import { AcademicGovernancePage } from './AcademicGovernancePage'
const governanceHook=vi.hoisted(()=>({useAcademicGovernance:vi.fn()}));vi.mock('../hooks/useAcademicGovernance',()=>governanceHook);afterEach(cleanup)
const state=()=>({semesters:[{id:1,organizationId:1,organizationCode:'FPTU',organizationName:'FPT University',code:'FA26',name:'Fall 2026',startDate:'2026-09-01',endDate:'2026-12-31',status:'UPCOMING',createdAt:'',updatedAt:''}],periods:[{id:2,academicSemesterId:1,semesterCode:'FA26',semesterName:'Fall 2026',code:'REG-01',name:'Registration',periodType:'REGISTRATION',startAt:'2026-09-01T00:00',endAt:'2026-09-10T00:00',status:'DRAFT',minTeamSize:3,maxTeamSize:5,minDistinctMajors:1,maxProjectsPerSupervisor:5,milestoneTemplateId:null,rubricId:null,createdAt:'',updatedAt:''}],error:null,loading:false,isForbidden:false,isUnauthorized:false,canManage:true,submitting:false,retry:vi.fn(),saveSemester:vi.fn(),saveProjectPeriod:vi.fn(),setStatus:vi.fn()})
beforeEach(()=>governanceHook.useAcademicGovernance.mockImplementation(()=>state()))
describe('AcademicGovernancePage',()=>{it('renders semester ownership and period policy together',()=>{render(<AcademicGovernancePage/>,{wrapper:MemoryRouter});expect(screen.getByText('Fall 2026')).toBeDefined();expect(screen.getByText('Registration')).toBeDefined();expect(screen.getByText('Team size')).toBeDefined();expect(screen.getByText('Supervisor capacity')).toBeDefined();expect(screen.getByText(/Project-specific mode, lead department and MajorRequirements remain on the Team\/Project academic scope/)).toBeDefined()});it('exposes edit only while the backend permits mutation',()=>{render(<AcademicGovernancePage/>,{wrapper:MemoryRouter});expect(screen.getAllByRole('button',{name:'Chỉnh sửa'})).toHaveLength(2)});it('does not present an unavailable transition for archived records',()=>{governanceHook.useAcademicGovernance.mockReturnValue({...state(),semesters:[{...state().semesters[0],status:'ARCHIVED'}],periods:[]});render(<AcademicGovernancePage/>,{wrapper:MemoryRouter});expect(screen.queryByRole('button',{name:'Chỉnh sửa'})).toBeNull();expect(screen.queryByRole('button',{name:'UPCOMING'})).toBeNull()});it('keeps unauthorized access on the existing login path',()=>{governanceHook.useAcademicGovernance.mockReturnValue({...state(),isUnauthorized:true});render(<AcademicGovernancePage/>,{wrapper:MemoryRouter});expect(screen.getByRole('link',{name:'Đăng nhập'})).toBeDefined()})})
